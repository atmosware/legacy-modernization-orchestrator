{
  "skill": "go-gin-fiber",
  "version": "1.0.0",
  "generated_from": "SKILL.md — run 'node scripts/generate-dod.js' to regenerate",
  "items": [
    {
      "id": "GGF-01",
      "description": "`go build ./...` exits with zero errors",
      "verifier": "manual"
    },
    {
      "id": "GGF-02",
      "description": "`go vet ./...` exits with zero warnings",
      "verifier": "cmd"
    },
    {
      "id": "GGF-03",
      "description": "`golangci-lint run` exits with zero warnings (`errcheck`, `staticcheck`, `gosec`, `revive` enabled)",
      "verifier": "manual"
    },
    {
      "id": "GGF-04",
      "description": "`go test -coverprofile=coverage.out ./...` achieves ≥ 70% coverage",
      "verifier": "cmd"
    },
    {
      "id": "GGF-05",
      "description": "`go test -race ./...` exits with zero race conditions detected",
      "verifier": "cmd"
    },
    {
      "id": "GGF-06",
      "description": "`govulncheck ./...` reports zero known vulnerabilities",
      "verifier": "manual"
    },
    {
      "id": "GGF-07",
      "description": "`/health/live` returns `{\"status\":\"ok\"}` in local run",
      "verifier": "manual"
    },
    {
      "id": "GGF-08",
      "description": "`/health/ready` returns `{\"status\":\"ok\"}` (DB ping confirmed)",
      "verifier": "manual"
    },
    {
      "id": "GGF-09",
      "description": "`/metrics` returns Prometheus-format metrics",
      "verifier": "manual"
    }
  ]
}
